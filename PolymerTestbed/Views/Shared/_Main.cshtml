@using System.Web.Optimization
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Scripts.Render("~/polymer")

    @if (IsSectionDefined("PolymerImports"))
    {
        @RenderSection("PolymerImports")
    }

</head>
<body>
    @RenderBody()
</body>
</html>
